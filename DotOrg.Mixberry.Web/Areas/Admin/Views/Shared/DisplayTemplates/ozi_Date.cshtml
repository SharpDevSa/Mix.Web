@using DateSettings = DotOrg.Web.Ozi.PagesSettings.Lists.DateSettings
@{
    var colSettings = ViewData["ColSettings"] as DateSettings;
}
@DateTime.Parse(ViewData.TemplateInfo.FormattedModelValue.ToString()).ToString(colSettings.Format)
